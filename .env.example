# Copy to .env and fill in values. Never commit .env.

# --- Contracts ---
RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY
# Local-only: for `npm run deploy:sepolia` outside the ceremony. In CI the key
# is never stored — it is reconstructed from the reviewer shares. Never commit.
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=

# --- Frontend ---
VITE_TOKEN_ADDRESS=
VITE_DAO_ADDRESS=
VITE_CHAIN_ID=11155111      # Sepolia
